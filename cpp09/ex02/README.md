1 pair recursively ()
 - check if everything in the main container possible
 - check how labeling works in the recursive call
 - check how to store all the pairs list

2 create a main and pend vector
3 insert pend in main with jacobstal suite